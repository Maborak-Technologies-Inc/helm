{{- if and .Values.redis.enabled .Values.redis.networkPolicy.enabled }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "amazon-watcher-stack.fullname" . }}-redis-networkpolicy
  labels:
    {{- include "amazon-watcher-stack.labels" . | nindent 4 }}
    app.kubernetes.io/component: redis
spec:
  podSelector:
    matchLabels:
      {{- include "amazon-watcher-stack.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: redis
  policyTypes:
    - Ingress
  ingress:
    # Allow traffic from Backend
    - from:
        - podSelector:
            matchLabels:
              {{- include "amazon-watcher-stack.selectorLabels" . | nindent 14 }}
              app.kubernetes.io/component: backend
      ports:
        - protocol: TCP
          port: {{ .Values.redis.port }}
    # Allow traffic from Backend CLI
    - from:
        - podSelector:
            matchLabels:
              {{- include "amazon-watcher-stack.selectorLabels" . | nindent 14 }}
              app.kubernetes.io/component: backend-cli
      ports:
        - protocol: TCP
          port: {{ .Values.redis.port }}
    # Allow traffic from Backend CronJob
    - from:
        - podSelector:
            matchLabels:
              {{- include "amazon-watcher-stack.selectorLabels" . | nindent 14 }}
              app.kubernetes.io/component: backend-cronjob
      ports:
        - protocol: TCP
          port: {{ .Values.redis.port }}
    # Allow traffic from Maborak (utility pod)
    - from:
        - podSelector:
            matchLabels:
              {{- include "amazon-watcher-stack.selectorLabels" . | nindent 14 }}
              app.kubernetes.io/component: maborak
      ports:
        - protocol: TCP
          port: {{ .Values.redis.port }}
    # Custom ingress rules
    {{- if .Values.redis.networkPolicy.ingress }}
    {{- toYaml .Values.redis.networkPolicy.ingress | nindent 4 }}
    {{- end }}
{{- end }}
