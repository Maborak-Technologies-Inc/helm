{{- $hpaEnabled := .Values.ui.autoscaling.enabled }}
{{- if ne (printf "%v" .Values.global.hpa) "" }}
  {{- $hpaEnabled = .Values.global.hpa }}
{{- end }}
{{- if eq (printf "%v" .Values.ui.autoscaling.enabled) "false" }}
  {{- $hpaEnabled = false }}
{{- end }}
{{- if and .Values.ui.enabled $hpaEnabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "tiktok-analytics-stack.fullname" . }}-ui
  labels:
    {{- include "tiktok-analytics-stack.labels" . | nindent 4 }}
    app.kubernetes.io/component: ui
  {{- if .Values.argocd.annotations }}
  annotations:
    {{- include "tiktok-analytics-stack.argocd.annotations" . | nindent 4 }}
  {{- end }}
spec:
  scaleTargetRef:
    apiVersion: argoproj.io/v1alpha1
    kind: Rollout
    name: {{ include "tiktok-analytics-stack.fullname" . }}-ui
  minReplicas: {{ .Values.ui.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.ui.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.ui.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.ui.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.ui.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.ui.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
  {{- if .Values.ui.autoscaling.behavior }}
  behavior:
    {{- toYaml .Values.ui.autoscaling.behavior | nindent 4 }}
  {{- end }}
{{- end }}
