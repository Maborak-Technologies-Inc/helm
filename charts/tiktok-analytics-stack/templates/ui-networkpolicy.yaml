{{- if and .Values.ui.enabled .Values.ui.networkPolicy.enabled }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "tiktok-analytics-stack.fullname" . }}-ui
  labels:
    {{- include "tiktok-analytics-stack.labels" . | nindent 4 }}
    app.kubernetes.io/component: ui
  {{- if .Values.argocd.annotations }}
  annotations:
    {{- include "tiktok-analytics-stack.argocd.annotations" . | nindent 4 }}
  {{- end }}
spec:
  podSelector:
    matchLabels:
      {{- include "tiktok-analytics-stack.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: ui
  policyTypes:
    - Ingress
    - Egress
  {{- if .Values.ui.networkPolicy.ingress }}
  ingress:
    {{- toYaml .Values.ui.networkPolicy.ingress | nindent 4 }}
  {{- else }}
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              name: ingress-nginx
      ports:
        - protocol: TCP
          port: 80
  {{- end }}
  {{- if .Values.ui.networkPolicy.egress }}
  egress:
    {{- toYaml .Values.ui.networkPolicy.egress | nindent 4 }}
  {{- else }}
  egress:
    - to:
        - podSelector:
            matchLabels:
              {{- include "tiktok-analytics-stack.selectorLabels" . | nindent 14 }}
              app.kubernetes.io/component: backend
      ports:
        - protocol: TCP
          port: {{ .Values.backend.env.PHOVEUS_BACKEND_UVI_PORT | default "9020" | int }}
    - to:
        namespaceSelector: {}
      ports:
        - protocol: UDP
          port: 53
        - protocol: TCP
          port: 443
  {{- end }}
{{- end }}
