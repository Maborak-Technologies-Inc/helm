apiVersion: v1
kind: Service
metadata:
  name: {{ include "amazon-watcher-backend.fullname" . }}
  labels:
    {{- include "amazon-watcher-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "amazon-watcher-backend.selectorLabels" . | nindent 4 }}

